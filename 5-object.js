Object.create
Object.defineProperty
Object.assign

Object.keys

// ES2017
// npm install object.entries object.values
if ( !Object.entries ) { require('object.entries').shim() }
if ( !Object.values ) { require('object.values').shim() }

Object.entries
Object.values
